# graphify scope — keep the knowledge graph focused on the main
# BS(n+1,n) base-sequence δ-code SOLVER work.
#
# The Sarukhanian δ-code CONSTRUCTION (length-110) is a SEPARATE sub-project,
# self-contained under sarukhanian/, and deliberately excluded here.
# Build a standalone graph for it any time with:  /graphify sarukhanian
sarukhanian/

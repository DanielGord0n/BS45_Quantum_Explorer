# Machine-readable seed ledger for the autonomous loop.
# next_seeds.sh reads and advances this so the MODEL never picks seeds
# (a reused base = identical SA trajectories = a wasted cluster round).
#
# Base-audit 2026-07-11 (first autonomous run): HANDOFF rounds 4-5 had already
# burned 60M/63M (Fir 47870263 / Rorqual 15587013) and 66M/69M/72M
# (Fir 48072964 / Rorqual 15719455 / Nibi 17483618), so NEXT_BASE was
# corrected 60M -> 75M before the first take.
#
# Base-audit 2026-07-11 (run 2): bases 75M/78M/81M (run-1 paste block, never
# pasted) and 84M/87M/90M (allocated but never submitted) are RETIRED UNUSED —
# do not resurrect them; round 6 actually flew on 93M/96M/99M
# (Fir 48213931 / Rorqual 15754557 / Nibi 17500261).
#
# Base-audit 2026-07-13 (run 5): 114M = Rorqual 15989201, 117M = Nibi 17557893
# (both echoed job IDs). 111M was allocated for Fir but the loop's submit FAILED 3x
# (Fir Duo pushes went unanswered while rorqual/nibi pushes in between were
# approved — Fir-side push-flow problem, not an away user).
#
# UPDATE 2026-07-13 (evening): 111M IS NOW USED — Daniel pasted the block by hand and
# Fir echoed job **48551063**. It is NOT retired; do not hand it out again. (The earlier
# "retire if unpasted" note above is superseded.)
#
# SEED = NEXT_BASE + ARRAY_TASK_ID*100000 inside cluster_sa_ladder.sh.
# Bases are handed out STRIDE apart, one per cluster, never reused.

N=34
NEXT_BASE=135000000
STRIDE=3000000
